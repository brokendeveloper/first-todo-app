services:
  seq:
    image: datalust/seq:latest
    container_name: seq-for-api
    environment:
      - ACCEPT_EULA=Y
      - SEQ_FIRSTRUN_ADMINPASSWORD=my-password-strong
    ports:
      - "5341:80"
    volumes:
      - seq-data:/data

volumes:
  seq-data: