services:
  postgres:
    image: postgres:15
    container_name: api-todo-container
    environment:
      - POSTGRES_PASSWORD=mystrongpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: